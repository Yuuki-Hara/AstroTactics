# 🚀 宇宙SFローグライクカードゲーム 基本設計書

## 1. ゲーム概要

- **テーマ**: 宇宙戦争。プレイヤーは戦艦の司令官となり、エイリアン軍団と戦う。
- **コアループ**: 星図（マップ）を進行し、戦闘に勝利して戦艦の兵装（カード）やロボットを回収・強化しながら奥深くへと進んでいく。
- **戦闘システム**: ターン制のカードバトル。毎ターン回復するエネルギー（EN）を消費してカードを使用する。

## 2. カード設計（Card Data Structure）

カードの基本データと、それが持つ属性・効果の定義。

### 2.1 カード本体（Card）

| プロパティ名 | データ型     | 説明                                    |
| :----------- | :----------- | :-------------------------------------- |
| `id`         | UUID         | カードのユニークID                      |
| `baseId`     | String       | カードの種類識別ID（例: `main_cannon`） |
| `name`       | String       | カード名                                |
| `cost`       | Int          | 消費エネルギー（EN）                    |
| `traits`     | [CardTrait]  | カードの属性（複数可）                  |
| `target`     | TargetType   | 効果の対象                              |
| `effects`    | [CardEffect] | 発動効果（複数可）                      |
| `isUpgraded` | Bool         | 強化済みフラグ                          |

### 2.2 列挙型定義

- **CardTrait（属性）**: `beam` (光学兵器), `missile` (実弾), `mech` (ロボット), `defense` (防御), `command` (司令)
- **TargetType（対象）**: `singleEnemy`, `allEnemies`, `randomEnemy`, `selfTarget`
- **CardEffect（効果）**: `dealDamage(amount)`, `gainShield(amount)`, `drawCard(count)`, `heal(amount)`, `applyStatus(status, amount)`
- **StatusType（状態異常）**: `emp` (行動阻害), `target` (被ダメ増), `evasion` (回避率アップ)

## 3. デッキ管理設計（BattleDeckManager）

戦闘中のカードの移動と状態を管理するシステム。

### 3.1 プロパティ

- `drawPile: [Card]` (山札)
- `hand: [Card]` (手札)
- `discardPile: [Card]` (捨て札)
- `exhaustPile: [Card]` (除外/使い切り)

### 3.2 メソッド

- `drawCard(count: Int)`: 山札からカードを引く（山札ゼロ時は捨て札をリシャッフル）
- `playCard(card: Card, target: TargetType)`: カードを使用し、捨て札（または除外）へ送る
- `discardHand()`: ターン終了時に手札をすべて捨て札へ送る
- `shuffleDiscardIntoDraw()`: 捨て札をシャッフルして山札に戻す
- `exhaustCard(card: Card)`: カードを除外パイルへ送る

## 4. キャラクター設計（Combat Entities）

自艦と敵のステータス管理。

### 4.1 共通仕様（CombatEntity Protocol）

- **プロパティ**: `name` (名前), `maxHP`, `currentHP`, `shield` (装甲), `statuses` (状態異常リスト)
- **メソッド**: `takeDamage(amount: Int)` (シールドから減算し、超過分をHPから減算)

### 4.2 自艦（PlayerShip）

- **追加プロパティ**: `maxEnergy`, `currentEnergy`
- **追加メソッド**: `resetEnergy()`

### 4.3 敵（Enemy）

- **追加プロパティ**: `id` (敵ID), `intent` (EnemyIntent: 次の行動予告)
- **追加メソッド**: `determineNextIntent()`
- **EnemyIntent定義**: `attack(damage)`, `defend(amount)`, `buff`, `debuff(status)`

## 5. バトル進行設計（BattleManager）

ターンの進行と勝敗判定を管理する総司令塔。

### 5.1 バトル状態（BattleState）

1. `battleStart` (戦闘開始)
2. `playerTurnStart` (自艦ターン開始)
3. `playerAction` (自艦アクション中 / 入力待ち)
4. `playerTurnEnd` (自艦ターン終了)
5. `enemyTurnStart` (敵ターン開始)
6. `enemyAction` (敵アクション中)
7. `enemyTurnEnd` (敵ターン終了)
8. `victory` (勝利) / `defeat` (敗北)

### 5.2 プロパティとメソッド

- **プロパティ**: `currentState`, `player`, `enemies`, `deckManager`, `turnCount`
- **メソッド**:
  - `changeState(to: BattleState)`: 状態遷移と自動処理の実行
  - `endPlayerTurn()`: プレイヤーのターン終了トリガー
  - `checkWinCondition()`: 勝敗の判定と状態の更新
