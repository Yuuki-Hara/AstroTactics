# 🚀 AstroTactics プロジェクト設計書 (Ver. Prototype)

## 1. プロジェクト概要

宇宙SFを舞台にした、ローグライク・デッキ構築型カードゲーム。
プレイヤーは自艦（アストロ旗艦）を操り、毎ターン回復するエネルギー（EN）を消費して手札からカードを使用し、ランダムに出現するエイリアン艦隊とターン制のバトルを行う。

## 2. ディレクトリ・ファイル構成

クリーンアーキテクチャを意識した、役割ごとの明確な分離を行っている。

```text
AstroTactics/
├── App/
│   └── AstroTacticsApp.swift     (アプリの起動口)
├── Views/
│   ├── ContentView.swift         (画面の親玉・ゲームループ管理)
│   └── Battle/
│       ├── BattleView.swift      (メインの戦闘UI)
│       └── CardView.swift        (カード単体のUIとテキスト生成)
├── Models/
│   ├── Core/
│   │   └── CombatEntity.swift    (戦闘キャラの共通ルール/プロトコル)
│   └── Battle/
│       ├── PlayerShip.swift      (自艦のデータ)
│       ├── Enemy.swift           (敵のデータとAI思考)
│       ├── Card.swift            (カードの構成データ)
│       └── BattleState.swift     (バトルの進行フェーズ定義)
├── Managers/
│   ├── BattleManager.swift       (戦闘の進行・ダメージ計算・演出の総司令塔)
│   └── BattleDeckManager.swift   (山札・手札・捨て札の管理)
└── Data/
    ├── CardDatabase.swift        (登場する全カードのカタログ)
    └── EnemyDatabase.swift       (登場する全敵キャラの図鑑)
```

## 3. コアデータモデル

### キャラクターシステム（CombatEntity）

敵と味方で共通する「HP・シールド・状態異常」のステータスと、ダメージ計算のルール（`takeDamage`）を統一して管理。

| モデル名       | 役割・特徴         | 独自プロパティ・メソッド                             |
| :------------- | :----------------- | :--------------------------------------------------- |
| **PlayerShip** | プレイヤー（自艦） | 最大EN、現在ENの管理、ターン開始時のENリセット       |
| **Enemy**      | 敵キャラクター     | `id`によるAI思考分岐、`intent`（次の行動予定）の決定 |

### カードシステム（Card）

カード1枚が持つ情報を細かく分解し、様々な効果の組み合わせを実現。

| 要素           | 役割     | 種類・詳細                                             |
| :------------- | :------- | :----------------------------------------------------- |
| **CardTrait**  | 属性     | 光学、実弾、機体、装甲、指令、無属性                   |
| **TargetType** | 対象     | 敵単体、敵全体、ランダムな敵、自身                     |
| **CardEffect** | 発動効果 | ダメージ、シールド付与、ドロー、回復、状態異常付与     |
| **StatusType** | 状態異常 | EMP（行動阻害）、ターゲット（被ダメ1.5倍）、回避アップ |

## 4. バトルシステム・状態遷移

`BattleManager`が`@Observable`を用いて画面と連携し、非同期処理（`async/await`）によって「間（ま）」のある演出を制御。

1. **戦闘開始 (`.battleStart`)**: 初期ドロー、敵の初回行動決定。
2. **プレイヤーターン開始 (`.playerTurnStart`)**: ENリセット、シールド解除。
3. **プレイヤー行動中 (`.playerAction`)**: カードのプレイ、効果の順次発動、勝敗チェック。
4. **プレイヤーターン終了 (`.playerTurnEnd`)**: 手札の破棄、プレイヤーの状態異常カウントダウン。
5. **敵ターン開始 (`.enemyTurnStart`)**: 敵のシールド解除。
6. **敵行動中 (`.enemyAction`)**: AIによる攻撃または防御の発動、勝敗チェック。
7. **敵ターン終了 (`.enemyTurnEnd`)**: 次の行動予定決定、敵の状態異常カウントダウン、手札を5枚ドロー。

## 5. UIとゲームループ

- **リアクティブUI**: `@Observable`により、HP増減、カード使用、メッセージ変更が即座に`BattleView`に反映される。
- **ゲームループの実現**: `ContentView`にて`.id(ObjectIdentifier(battleManager))`を使用。勝敗決定後の「もう一度遊ぶ」ボタン押下時、データを丸ごとすり替えることで、SwiftUIの機能により画面を完全リセットして再スタートする。
